---
version: 5
defaults:
  datadir: hiera
  data_hash: yaml_data

hierarchy:

    - name: Role
      paths:
        - 50_role/%{trusted_facts.extensions.pp_role}/%{trusted_facts.extensions.pp_environment}.yaml
        - 50_role/%{trusted_facts.extensions.pp_role}/_default.yaml

    - name: Global
      paths:
        - common.yaml
